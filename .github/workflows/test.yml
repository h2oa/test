name: ci

on:
  pull_request:
    branches: [master]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: pull_request on master detection
        run: |
          echo "Detect pull request on master!"
